#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Description: Using the "puts" function to print out an array of characters.
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}

