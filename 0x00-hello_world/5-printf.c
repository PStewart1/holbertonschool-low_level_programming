#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Description: Using the "printf" function to print out a string.
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}

