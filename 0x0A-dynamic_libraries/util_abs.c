#include "main.h"

/**
 * _abs - returns absolute value of int
 * @n: integer to be checked
 *
 * Return: absolute value of int n
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n ==  0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

